# This file is committed and provides the build-time fallback for Vercel.
# Override by setting VITE_API_URL in the Vercel dashboard (Project → Settings → Environment Variables).
# The Vercel dashboard value always wins over this file.
VITE_API_URL=https://expenseflow-day02.onrender.com/api/v1
